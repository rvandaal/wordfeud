export class LetterTile {
    row: number;
    col: number;
    letter: string;

    constructor(row: number, col: number, letter: string) {
        this.row = row;
        this.col = col;
        this.letter = letter;
    }
}
